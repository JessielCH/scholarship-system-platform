# Default listener for internal network (TCP)
listener 1883
protocol mqtt
allow_anonymous true

# WebSockets listener for Frontend (Student Portal)
listener 9001
protocol websockets
allow_anonymous true
